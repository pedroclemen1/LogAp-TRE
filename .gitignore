# ---- Node / Next (web) ----
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# ---- Java / Maven (api) ----
target/
!.mvn/wrapper/maven-wrapper.jar
.mvn/timing.properties

# ---- Segredos ----
.env
.env.local
*.local

# ---- Editor / SO ----
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.swp

# ---- Logs ----
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---- Material de apoio, nao faz parte da solucao ----
# Enunciado do desafio (documento de quem avalia) e asset de design solto.
# Continuam no disco; so nao entram no repositorio.
desafio-descricao.pdf
logap_logo_refined.png

# ---- Lockfile duplicado ----
# O projeto usa npm; um yarn.lock aparece quando se roda `yarn` por engano.
yarn.lock
